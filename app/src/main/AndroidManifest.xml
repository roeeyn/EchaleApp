<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="bit01.com.mx.echale">

    <application
        android:allowBackup="true"
        android:icon="@drawable/coyotes"
        android:label="@string/app_name"
        android:roundIcon="@drawable/coyotes"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".ui.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.PartidosRecyclerViewActvity"
            android:label="@string/title_activity_partidos_recycler_view_actvity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".models.ApuestaActivity"></activity>
    </application>

</manifest>